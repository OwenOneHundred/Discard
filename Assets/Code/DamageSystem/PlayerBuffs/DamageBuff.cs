using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(menuName = "SpellBuff/DamageBuff")]
public class DamageBuff : SpellBuff
{
    // nothing here because it just makes damage buffs, which are included by default
}
